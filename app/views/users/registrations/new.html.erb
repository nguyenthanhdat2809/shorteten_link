<div class="login container bg-white rounded w-25 p-4 mt-4">
  <div class="row">
    <h2>Sign up</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "users/shared/error_messages", resource: resource %>

      <div class="form-group">
        <%= f.label :user_name %><br />
        <%= f.text_field :user_name, autofocus: true, autocomplete: "user_name", class: "form-control" %>
      </div>

      <div class="form-group mt-2">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
      </div>

      <!-- Profile model -->
      <%= f.fields_for :profile do |g| %>
        <div class="form-group mt-2">
          <%= g.label :first_name %><br />
          <%= g.text_field :first_name, autocomplete: "first name", class: "form-control" %>
        </div>
        <div class="form-group mt-2">
          <%= g.label :last_name %><br />
          <%= g.text_field :last_name, autocomplete: "last name", class: "form-control" %>
        </div>
        <div class="form-group mt-2">
          <%= g.label :phone_number %><br />
          <%= g.text_field :phone_number, autocomplete: "phone number", class: "form-control" %>
        </div>
        <div class="form-group mt-2">
          <%= g.label :card_id %><br />
          <%= g.text_field :card_id, autocomplete: "card id", class: "form-control" %>
        </div>
      <% end %>

      <div class="form-group mt-2">
        <%= f.label :password %>
        <%= f.password_field :password, autocomplete: "new-password", class: "form-control" %>
      </div>

      <div class="form-group mt-2">
        <%= f.label :password_confirmation %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
      </div>

      <div class="field mt-2">
        <div class="gravatar_edit">
          <%= f.label :avatar %><br />
          <%= f.file_field :avatar,
            placeholder: 'Upload picture',
            direct_upload: true,
            class: "form-control"
            %>
        </div>
      </div>

      <div class="actions mt-2">
        <%= f.submit "Sign up", class: "btn btn-primary" %>
      </div>
    <% end %>

    <div class="mt-2">
      <%= render "users/shared/links" %>
    </div>
  </div>
</div>
